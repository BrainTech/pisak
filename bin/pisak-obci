#!/usr/bin/env python3

"""
Script to run testing OBCI application.
"""

if __name__ == "__main__":
    import pisak
    from pisak import app_manager, res, handlers, obci_scanner
    from pisak.speller import widgets  # @UnusedImport
    import pisak.speller.handlers  # @UnusedImport

    pisak.init()

    def prepare_main_view(app, window, script, data):
        """
        View preparator.

        :param app: reference to the application, :see: :module:`pisak.application`.
        :param window: application main window, :class:`pisak.window.Window` instance.
        :param script: ClutterScript with the view description.
        :param data: some specific data.
        """
        handlers.button_to_view(window, script, "button_exit")

        keyboard = script.get_object('keyboard_panel')
        scanner = obci_scanner.Scanner(keyboard)
        dur = 5000
        scanner.run_scenario(
            [(('row', 'random-no-replacement'), dur),
            (('column', 'random-no-replacement'), dur),
            (('row+column', 'random-no-replacement'), dur),
            (('element', 'random-no-replacement'), dur),
            (('row', 'random-replacement-greedy'), dur),
            (('column', 'random-replacement-greedy'), dur),
            (('row+column', 'random-replacement-greedy'), dur),
            (('element', 'random-replacement-greedy'), dur)]
        )

    speller_app = {
        "app": "speller",
        "type": "clutter",
        "views": [("main", prepare_main_view)]
    }

    app_manager.run(speller_app)